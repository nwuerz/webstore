-- Drops Database if Exists
DROP DATABASE IF EXISTS zombie_db;

-- Create New Database
CREATE DATABASE zombie_db;

-- Use our new DB
-- USE zombie_db;

-- Create new table
-- CREATE TABLE categories (
    --category_id INT NOT NULL,
    --toiletries VARCHAR(30),
    --food VARCHAR(30),
    --medical_supplies VARCHAR(30),
    --weapons VARCHAR(30),
    --misc VARCHAR(30),
    --PRIMARY KEY(category_id)
--);

--CREATE TABLE items (
    --category_id INT NOT NULL,

    --PRIMARY KEY(category_id)
--);